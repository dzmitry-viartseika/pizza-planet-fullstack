const scrapRouter = require('../routers/scrap')
const goodsRouter = require('../routers/goods')
const userRouter = require('../routers/user')

module.exports = {
    scrapRouter,
    goodsRouter,
    userRouter,
}
